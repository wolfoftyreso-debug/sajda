# Copy this file to the root of the official self-hosted Supabase Docker
# project, then add it to COMPOSE_FILE in that project's .env. It injects only
# application secrets into the existing Edge Functions service.
services:
  functions:
    env_file:
      - .env.functions
